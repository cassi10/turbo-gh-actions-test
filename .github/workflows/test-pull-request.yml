on:
  pull_request:
    branches:
      - main

jobs:
  test:
    uses: ./.github/workflows/check-source-branch.yml
    with:
      base_ref: ${{ github.base_ref }}
      head_ref: ${{ github.head_ref }}
      wanted_head_ref: "nextty"
