name: Reusable workflow example

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  print_username:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.username }}
